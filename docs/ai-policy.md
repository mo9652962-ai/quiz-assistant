# AI 使用政策

AI 默认关闭。启用时只发送用户明确选择的题干、选项和上下文摘要；不执行模型返回的代码、URL、工具调用或文件操作。Provider 结果必须先通过 Pydantic schema 和选项业务校验，答案只展示为“候选答案/需要确认”，不会覆盖已有 active 答案。

网络失败、超时、限流或 JSON 无效只返回本地错误，不影响本地题库和练习。默认审计只保留结构化结果与哈希；原文是否落库由用户配置决定。

